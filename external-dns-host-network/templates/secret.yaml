apiVersion: v1
kind: Secret
metadata:
  name: {{ include "external-dns-host-network.fullname" . | quote }}
  labels:
  {{- include "external-dns-host-network.labels" . | nindent 4 }}
type: Opaque
data:
  LINODE_CLI_TOKEN: {{ .Values.settings.linodeToken | b64enc | quote }}
